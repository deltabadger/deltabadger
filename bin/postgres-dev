#!/usr/bin/env bash
 #
 # This file needs execution permission: `chmod +x bin/postgres-dev`

 PG_LOG_FILE="log/postgres-dev.log"

 stop_postgres() {
   pg_ctl stop
   exit 0
 }; trap stop_postgres INT TERM HUP QUIT KILL EXIT

 pg_ctl start -l "${PG_LOG_FILE}" && tail -f "${PG_LOG_FILE}"