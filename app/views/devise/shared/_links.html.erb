<div class="devise__links">
  <% if controller_name != 'sessions' %>
    <%= link_to t('links.log_in'), new_session_path(resource_name), class: "btn btn-link" %>
    <span class="db-middot">&middot;</span>
  <% end %>

  <% if devise_mapping.registerable? && controller_name != 'registrations' %>
    <%= link_to t('links.sign_up'), new_registration_path(resource_name), class: "btn btn-link" %>
    <span class="db-middot">&middot;</span>
  <% end %>

  <% if devise_mapping.confirmable? && controller_name != 'confirmations' %>
    <%= link_to t('links.resend_link'), new_confirmation_path(resource_name), class: "btn btn-link" %>
    <span class="db-middot">&middot;</span>
  <% end %>

  <% if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
    <%= link_to "Resend unlock instructions", new_unlock_path(resource_name), class: "btn btn-link" %>
    <span class="db-middot">&middot;</span>
  <% end %>

  <% if devise_mapping.omniauthable? %>
    <% resource_class.omniauth_providers.each do |provider| %>
      <%= link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider), class: "btn btn-link" %>
      <span class="db-middot">&middot;</span>
    <% end %>
  <% end %>

  <div data-controller="dropdown" data-action="click->dropdown#toggle" data-dropdown-target="wrapper" class="btn btn-link dropdown-wrapper dropdown-wrapper--right">
    <%= params[:locale]&.to_s&.upcase || 'EN' %>
    <div class="dropdown">
      <% locale = params[:locale] %>
      <% unless locale.blank? || locale == 'en' %>
        <%= link_to "🇬🇧 English", url_for(request.params.merge(locale: nil)), class: "dropdown__item" %>
      <% end %>
      <% unless locale == 'de' %>
        <%= link_to "🇩🇪 Deutsch", url_for(request.params.merge(locale: 'de')), class: "dropdown__item" %>
      <% end %>
      <% unless locale == 'nl' %>
        <%= link_to "🇳🇱 Nederlandse", url_for(request.params.merge(locale: 'nl')), class: "dropdown__item" %>
      <% end %>
      <% unless locale == 'fr' %>
        <%= link_to "🇫🇷 Français", url_for(request.params.merge(locale: 'fr')), class: "dropdown__item" %>
      <% end %>
      <% unless locale == 'es' %>
        <%= link_to "🇪🇸 Español", url_for(request.params.merge(locale: 'es')), class: "dropdown__item" %>
      <% end %>
      <% unless locale == 'pt' %>
        <%= link_to "🇵🇹 Português", url_for(request.params.merge(locale: 'pt')), class: "dropdown__item" %>
      <% end %>
      <% unless locale == 'pl' %>
        <%= link_to "🇵🇱 Polski", url_for(request.params.merge(locale: 'pl')), class: "dropdown__item" %>
      <% end %>
      <% unless locale == 'ru' %>
        <%= link_to "🇷🇺 Русский", url_for(request.params.merge(locale: 'ru')), class: "dropdown__item" %>
      <% end %>
    </div>
  </div>
</div>