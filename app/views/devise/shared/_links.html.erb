<div class="devise__links">
  <% if controller_name != 'sessions' %>
    <%= link_to t('links.log_in'), new_session_path(resource_name), class: "btn btn-link" %>
    <span class="db-middot">&middot;</span>
  <% end %>

  <% if devise_mapping.registerable? && controller_name != 'registrations' %>
    <%= link_to t('links.sign_up'), new_registration_path(resource_name), class: "btn btn-link" %>
    <span class="db-middot">&middot;</span>
  <% end %>

  <% if devise_mapping.confirmable? && controller_name != 'confirmations' %>
    <%= link_to t('links.resend_link'), new_confirmation_path(resource_name), class: "btn btn-link" %>
    <span class="db-middot">&middot;</span>
  <% end %>

  <% if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
    <%= link_to "Resend unlock instructions", new_unlock_path(resource_name), class: "btn btn-link" %>
    <span class="db-middot">&middot;</span>
  <% end %>

  <% if devise_mapping.omniauthable? %>
    <% resource_class.omniauth_providers.each do |provider| %>
      <%= link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider), class: "btn btn-link" %>
      <span class="db-middot">&middot;</span>
    <% end %>
  <% end %>

  <div class="btn-group">
    <button class="btn btn-link" aria-expanded="false" aria-haspopup="true" data-toggle="dropdown" type="button">
      <%= params[:locale]&.to_s&.upcase || 'EN' %>
      <svg class="ml-0 btn__svg-icon db-svg-icon" width="20" height="20" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
        <path d="M7.71,9.29l3.88,3.88l3.88-3.88c0.39-0.39,1.02-0.39,1.41,0l0,0c0.39,0.39,0.39,1.02,0,1.41l-4.59,4.59 c-0.39,0.39-1.02,0.39-1.41,0L6.29,10.7c-0.39-0.39-0.39-1.02,0-1.41l0,0C6.68,8.91,7.32,8.9,7.71,9.29z"></path>
      </svg>
    </button>
    <div class="dropdown-menu dropdown-menu-right">
      <% locale = params[:locale] %>
      <% unless locale.blank? || locale == 'en' %>
        <%= link_to "🇬🇧 English", url_for(request.params.merge(locale: nil)), class: "dropdown-item" %>
      <% end %>
      <% unless locale == 'de' %>
        <%= link_to "🇩🇪 Deutsch", url_for(request.params.merge(locale: 'de')), class: "dropdown-item" %>
      <% end %>
      <% unless locale == 'nl' %>
        <%= link_to "🇳🇱 Nederlandse", url_for(request.params.merge(locale: 'nl')), class: "dropdown-item" %>
      <% end %>
      <% unless locale == 'fr' %>
        <%= link_to "🇫🇷 Français", url_for(request.params.merge(locale: 'fr')), class: "dropdown-item" %>
      <% end %>
      <% unless locale == 'es' %>
        <%= link_to "🇪🇸 Español", url_for(request.params.merge(locale: 'es')), class: "dropdown-item" %>
      <% end %>
      <% unless locale == 'pt' %>
        <%= link_to "🇵🇹 Português", url_for(request.params.merge(locale: 'pt')), class: "dropdown-item" %>
      <% end %>
      <% unless locale == 'pl' %>
        <%= link_to "🇵🇱 Polski", url_for(request.params.merge(locale: 'pl')), class: "dropdown-item" %>
      <% end %>
      <% unless locale == 'ru' %>
        <%= link_to "🇷🇺 Русский", url_for(request.params.merge(locale: 'ru')), class: "dropdown-item" %>
      <% end %>
    </div>
  </div>
</div>