<h1><%= t('.subject') %></h1>

<p><%= t('mailer.greeting_name', name: @resource.name) %></p>
<%= t('.template_html') %>

<p>
  <%= link_to t('.confirm_button'), confirmation_url(@resource, confirmation_token: @token) %>
</p>
