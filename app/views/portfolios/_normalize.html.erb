<%= turbo_frame_tag "normalize" do %>
  <% unless portfolio.allocations_are_normalized? || portfolio.assets.empty? %>
    <%= button_to "Normalize and simulate", portfolio_normalize_allocations_path(portfolio), method: :post %>
  <% end %>
<% end %>
