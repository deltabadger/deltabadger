<%= turbo_stream.replace "risk-free-rate", partial: "risk_free_rate", locals: { portfolio: @portfolio } %>
<% if @portfolio.allocations_are_normalized? %>
  <%= render_turbo_stream_backtest_results(@backtest, @portfolio) %>
<% end %>
<% if @portfolio.smart_allocation_on? %>
  <%= turbo_stream.replace "smart-allocation-controls", partial: "smart_allocation_controls", locals: { portfolio: @portfolio } %>
  <%= render_turbo_stream_portfolio_assets(@portfolio, @last_active_assets_ids, @last_idle_assets_ids) %>
<% end %>
<%= render_turbo_stream_flash_messages %>
