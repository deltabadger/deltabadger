<%= turbo_frame_tag "portfolio-empty" do %>
  <% if portfolio.assets.empty? %>
    <%# better be a flash message? %>
    <p>Your Portfolio is empty. Add some assets to get started.</p>
  <% end %>
<% end %>