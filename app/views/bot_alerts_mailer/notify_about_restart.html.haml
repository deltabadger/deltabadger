%h1{class: "mail-header"}= t('.subject')

%p= t('mailer.greeting', name: @resource.name)
= t('.template_html', delay: @delay, exchange: @exchange_name, base: @bot.base, quote: @bot.quote, errors: @errors.inspect)
