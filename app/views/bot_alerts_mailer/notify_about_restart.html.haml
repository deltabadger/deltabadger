%h1= t('.subject')

%p= t('mailer.greeting_name', name: @user.name)
= t('.template_html', delay: time_ago_in_words(@restart_at), exchange_name: @exchange_name, errors: @errors.inspect, label: @label)
