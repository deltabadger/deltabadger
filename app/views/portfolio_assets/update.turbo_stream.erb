<% @portfolio.reload %>
<% if @portfolio.allocations_are_normalized? %>
  <%= turbo_stream_replace_backtest_results(@backtest, @portfolio) %>
<% end %>
<%= turbo_stream.replace "normalize-button", partial: "portfolios/normalize_button", locals: { portfolio: @asset.portfolio } %>
<%= turbo_stream_prepend_flash %>