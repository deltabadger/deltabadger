<div class="page-head">
  <div class="stitle"><%= t('links.api') %></div>
</div>

<%# The two surfaces side by side. Their frames are the grid items — no .column wrappers — so
    the pair stretches to one height (see .columns--stretch). %>
<div class="columns columns--stretch">
  <%= render partial: 'settings/widgets/mcp' %>
  <%= render partial: 'settings/widgets/rest' %>
</div>

<%= render partial: 'settings/widgets/tool_permissions' %>
<%= render partial: 'settings/widgets/connected_clients' %>
