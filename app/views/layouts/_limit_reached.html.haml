- if current_user.show_limit_reached_navbar?
  .container
    .alert.alert-primary.db-alert--flex
      %span
        <b>Congrats!</b> You have reached our free annual limit of $1000. Upgrade to the unlimited version.
      %a.btn.btn-primary{href: upgrade_path}
        %span Upgrade
