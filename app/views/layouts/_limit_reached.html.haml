- if current_user.limit_reached?
  .container
    .alert.alert-primary.db-alert--flex
      %span
        <b>Congrats!</b> You have reached our free annual limit of $1000. Support the project and unlock the unlimitted version.
      %button(type="button" class="btn btn-outline-primary")
        Upgrade
