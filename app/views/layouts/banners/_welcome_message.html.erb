<% if @show_welcome_banner %>
  <div data-controller="removals">
    <div class="alertbox alertbox--primary alertbox--welcome" style="margin-bottom: 2rem">
      <div style="width: 100%">
        <div class="alertbox__text-column">
          <p>Hi <%= current_user.name %>, Deltabadger does three things:</p>
          <ul>
            <li>Dollar-Cost Averaging bots</li>
            <li>Portfolio Analysis</li>
            <li>Portfolio Tracking (soon)</li>
          </ul>
          <p>It's simple:</p>
          <p>First, you acquire assets, and there is no better way to do it than dollar-cost averaging. <b>The&nbsp;first bot is now completely free</b>.</p>
          <p>Then, find the optimal allocation of your assets with our Analyzer and <b>Smart Allocation</b>.</p>
          <p>Finally, track all your holdings from DeFi to ETFs in one place. (That part is coming soon.)</p>
          <p>If you like what you see, consider joining Legendary Badgers — an <b>exclusive community of 1,000 lifetime subscribers</b>. This is an offer like no other that supports the project and gives a unique opportunity to become a lifetime beneficiary of all Deltabadger's superpowers.</p>
          <p style="margin-bottom:0">Best, Jan</p>
          <img src="https://deltabadger.com/images/signature-white.webp" alt="signature" style="width: 15rem" %>
          <%# t('welcome.banner_html', name: current_user.name) %>
        </div>
      </div>
      <%= button_to settings_hide_welcome_banner_path, method: :patch, title: "Close", class: "close", data: { action: "click->removals#submitAndRemove" } do %>
        <%= render partial: 'svg/24x24_x' %>
      <% end %>
      <%= render partial: 'layouts/svg_bubbleart_01' %>
    </div>
  </div>
<% end %>
