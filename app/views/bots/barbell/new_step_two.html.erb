<%= render 'layouts/modal/full_page' do %>
  <%= render partial: 'bots/barbell/progress_bar', locals: {
    current_step: :currencies
  } %>
  <div class="conversational conversational--bot-setup">Invest in <%= @bot.base0_asset.symbol %> and …</div>

  <%= render partial: 'bots/barbell/asset_search', locals: {
    bot: @bot,
    asset_field: :base1_asset_id,
    search_form_path: barbell_new_step_two_bots_path,
    next_step_path: barbell_new_step_three_bots_path,
    assets: @assets
  } %>
<% end %>
