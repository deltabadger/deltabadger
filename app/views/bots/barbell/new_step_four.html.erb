<%= render 'layouts/modal/full_page' do %>
  <%= render partial: 'bots/barbell/progress_bar', locals: {
    current_step: :exchange
  } %>
  <div class="conversational conversational--bot-setup">Invest in <div class="ticker" style="background: <%= @bot.base0_asset.color %>"><%= @bot.base0_asset.symbol %></div> and <div class="ticker" style="background: <%= @bot.base1_asset.color %>"><%= @bot.base1_asset.symbol %></div> at <%= @bot.exchange.name.capitalize %> spending …</div>

<%= @bot.inspect%>
  <%= render partial: 'bots/barbell/asset_search', locals: {
    bot: @bot,
    asset_field: :quote_asset_id,
    search_form_path: barbell_new_step_four_bots_path,
    next_step_path: barbell_new_step_five_bots_path,
    assets: @assets
  } %>

<% end %>