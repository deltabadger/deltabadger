<%= render 'layouts/modal/base' do %>
  <div class="modal">
    <%= button_tag type: 'button', class: 'modal__close', data: { action: 'modal--base#animateOutCloseAndCleanUp' } do %>
      <%= render partial: 'svg/24x24_close' %>
    <% end %>
    <div class="modal__header">
       <h1 class="modal__title"><%= t('utils.change_name') %></h1>
    </div>

    <%= form_with model: @bot, url: bot_path(@bot), method: :patch, class: "modal__form", data: { controller: "form--label-animations", action: "turbo:submit-end->modal--base#submitEnd" } do |f| %>
      <div class="form__row form__row--input">
        <%= f.text_field :label, autofocus: true, data: { controller: "autofocus" }, class: "form__input" %>
        <%= f.label :label, t('bot.bot_name'), class: "form__label" %>
      </div>
      <%= f.submit t('button.save'), class: "button button--success button--large" %>
    <% end %>

  </div>
<% end %>
