<%= render 'layouts/modal/base' do %>
  <div class="modal modal--xlarge">
    <%= button_tag type: 'button', class: 'modal__close', data: { action: 'modal--base#animateOutCloseAndCleanUp' } do %>
      <%= render partial: 'svg/24x24_close' %>
    <% end %>
    <div class="modal__header">
       <h1 class="modal__title"><%= t('button.start') %></h1>
    </div>

    <div class="itiles">

        <%= link_to new_bots_dca_single_assets_pick_buyable_asset_path, class: 'itile itile--bot-type', data: { turbo_frame: "modal" } do %>
            <div class="index-title"><%= t('bot.types.dca.title') %></div>
            <div class="index-info"><%= t('bot.types.dca.description').html_safe %></div>
        <% end %>

        <%= link_to new_bots_dca_dual_assets_pick_first_buyable_asset_path, class: 'itile itile--bot-type', data: { turbo_frame: "modal" } do %>
            <div class="index-title"><%= t('bot.types.dca_dual_asset.title') %></div>
            <div class="index-info"><%= t('bot.types.dca_dual_asset.description').html_safe %></div>
        <% end %>

        <%= link_to new_bots_dca_indexes_setup_coingecko_path, class: 'itile itile--bot-type', data: { turbo_frame: "modal" } do %>
            <div class="index-title"><%= t('bot.types.index_portfolio.title') %></div>
            <div class="index-info"><%= t('bot.types.index_portfolio.description').html_safe %></div>
        <% end %>

    </div>

  </div>
<% end %>
