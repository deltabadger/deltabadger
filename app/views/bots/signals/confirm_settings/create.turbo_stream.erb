<%= turbo_stream.replace 'new-settings', partial: 'bots/signals/settings', locals: { bot: @bot } %>
<%= turbo_stream.replace 'create-bot-button', partial: 'bots/signals/create_bot_button', locals: { bot: @bot } %>
<%= turbo_stream_prepend_flash %>
