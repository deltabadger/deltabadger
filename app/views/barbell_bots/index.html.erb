<h1>Barbell Bots</h1>

<%= link_to "New Barbell Bot", new_barbell_bot_path, class: "btn btn-primary" %>

<% @bots.each do |bot| %>
  <%= bot.id %>
  <%= link_to 'show', barbell_bot_path(bot) %>
<% end %>
