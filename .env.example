### internal config

# RAILS_ENV = "production"
# NODE_ENV = "production"

# In development mode, Rails automatically generates
# a SECRET_KEY_BASE derived from the app name, so you 
# don't need to set it explicitly, but for production, 
# generate with: 
#
# `openssl rand -hex 64`
#
# SECRET_KEY_BASE = ""
# DEVISE_SECRET_KEY = ""

RAILS_MAX_THREADS = 5  # Development: 5. Production: 10
WEB_CONCURRENCY = 0    # Development: 0. Production: Number of CPU Cores — 1
MAX_DB_CONNECTIONS = 5 # RAILS_MAX_THREADS * max(WEB_CONCURRENCY, 1)

# SMTP_ADDRESS = ""
# SMTP_PORT = ""
# SMTP_DOMAIN = ""
# SMTP_USER_NAME = ""
# SMTP_PASSWORD = ""
# SMTP_PROVIDER_NAME = ""  # Display name in Settings (e.g., "mail.example.com"). Falls back to SMTP_ADDRESS if not set.

DRY_RUN = false # Only affects Development. Production is always false, Test is always true
ORDERS_FREQUENCY_LIMIT = 60
# DEFAULT_MARKET_CACHING_TIME = ""

# Per-exchange proxy URLs for routing API traffic through specific IP addresses
# Used when exchanges require IP whitelisting for API access
# PROXY_BINANCE = "http://proxy.example.com:8100"
# PROXY_BINANCE_US = "http://proxy.example.com:8100"
# PROXY_COINBASE = "http://proxy.example.com:8100"
# PROXY_KRAKEN = "http://proxy.example.com:8100"
# PROXY_BITSTAMP = "http://proxy.example.com:8100"
# PROXY_KUCOIN = "http://proxy.example.com:8100"
# PROXY_PROBIT = "http://proxy.example.com:8100"
# PROXY_BITGET = "http://proxy.example.com:8100"
# PROXY_BYBIT = "http://proxy.example.com:8100"
# PROXY_MEXC = "http://proxy.example.com:8100"
# PROXY_GEMINI = "http://proxy.example.com:8100"
# PROXY_BITVAVO = "http://proxy.example.com:8100"

# NOTIFICATIONS_SENDER = "MyApp <noreply@example.com>"  # Optional: falls back to Gmail SMTP email or noreply@localhost
HOME_PAGE_URL = "http://localhost:3000"
APP_ROOT_URL = "http://localhost:3000"

# For production, generate with: `openssl rand -hex 32`
APP_ENCRYPTION_KEY = "aiD/O2fq61cu1nqWqN7u3He721VTR5+A"

# Optional: Add CoinGecko API key for fresh market data sync
# Without this key, the app uses pre-seeded fixture data (top 100 cryptocurrencies)
# Get your API key from: https://www.coingecko.com/en/api/pricing
# COINGECKO_API_KEY = ""

# Optional: Market Data API (set by launchpad for hosted instances)
# When configured, the app automatically syncs market data from this service instead of CoinGecko
# MARKET_DATA_URL = "https://market-data.example.com"
# MARKET_DATA_TOKEN = ""
# MARKET_DATA_PROVIDER_NAME = "Example"  # Display name in Settings. Falls back to MARKET_DATA_URL if not set.
